【DDD 规范：对象划分】
1. Cmd：写操作入参（CreateUserCmd、AuditOrderCmd）
2. Query：读操作入参（UserPageQuery）
3. VO：接口出参（UserVO、OrderDetailVO）
4. DTO：内部/第三方交互用
5. Entity：领域内部使用，绝不外泄
6. Converter 做映射：struct/mapstruct

禁止：
- Entity 直接返回前端
- Controller 直接 new Entity
- 一个类承担多种角色