【DDD 规范：四层架构】
1. Interface（对外）：Controller、VO、DTO、Converter
2. Application（协调）：XXXService、编排流程、事务、权限
3. Domain（核心）：Entity、ValueObject、DomainService、Repository 接口
4. Infrastructure（实现）：Mapper、RepositoryImpl、外部调用、MP 操作

依赖方向：
Interface → Application → Domain ← Infrastructure
禁止跨层跳级访问。