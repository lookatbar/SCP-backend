scp-basicdata
├── pom.xml
└── src/main/java/com/lookatbar/scp/xxx
├── xxxApplication.java          # 启动类
├── api/                               # 对外接口层（controller）
│   └── xxxController.java             # REST API
├── application/                       # 应用层（编排业务，无业务逻辑）
│   ├── dto/                            # 入参/出参 DTO
│   │   ├── xxxCreateDTO.java
│   │   └── xxxQueryDTO.java
│   ├── service/                       # 应用服务
│   │   └── xxxApplicationService.java
│   └── assembler/                     # DTO <-> 领域对象 转换
├── domain/                            # 领域层（核心业务）
│   ├── model/                          # 领域模型/实体
│   │   └── user/
│   │       ├── xxx.java              # 聚合根/实体
│   │       ├── xxxId.java            # 值对象
│   │       └── xxxStatus.java        # 值对象/枚举
│   ├── repository/                    # 领域仓储接口（只有interface）
│   │   └── xxxRepository.java
│   ├── service/                       # 领域服务（核心业务逻辑）
│   │   └── xxxDomainService.java
│   └── event/                         # 领域事件（可选）
├── infrastructure/                    # 基础设施层（技术实现）
│   ├── persistence/                   # 持久化实现
│   │   ├── mapper/                    # MyBatis/MP Mapper
│   │   │   └── xxxMapper.java
│   │   ├── po/                        # 数据库PO
│   │   │   └── xxxPO.java
│   │   └── repository/                # 仓储实现
│   │       └── xxxRepositoryImpl.java
│   ├── remote/                        # 远程调用（Feign）
│   └── config/                        # 配置类
└── common/                            # 模块内部通用
├── constant/                      # 常量
└── exception/                     # 异常