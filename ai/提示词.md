【DDD 强制红圈规则】
1. 严格四层架构：Application → Domain → Infrastructure → Interface
2. 核心逻辑只写在 Domain，禁止写在 Application
3. Entity 必须携带行为，禁止贫血模型
4. Repository 接口在 Domain，实现在 Infra
5. 事务在 Application 层
6. 禁止 Service 直接调用 Mapper
7. 入参 Cmd/Query，出参 VO，Entity 不对外
8. 业务规则显性表达，不藏在 SQL/MP 中
9. 生成后自动自检分层是否合规

【启用规范碎片】
分层架构 + Entity + Repository + Application + Cmd/Query/VO

【需求】
{在这里写需求}

【要求】
1. 严格按 DDD 四层结构生成代码
2. 使用 MyBatis-Plus 3.5.x 真实 API
3. 无幻觉、无编造、可直接运行
4. 自动自检，违规立即修正